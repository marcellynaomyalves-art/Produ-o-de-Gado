<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agro Forte & Futuro Sustentável - Simulador Pecuária 2026</title>
    <style>
        /* --- CONFIGURAÇÕES GERAIS --- */
        :root {
            --verde-escuro: #1b4d3e;
            --verde-claro: #27ae60;
            --marrom: #8b5a2b;
            --cinza-fundo: #f5f7f6;
            --card-fundo: #ffffff;
        }

        body {
            font-family: 'Segoe UI', system-ui, sans-serif;
            background-color: var(--cinza-fundo);
            color: #2c3e50;
            margin: 0;
            padding: 0;
        }

        header {
            background: linear-gradient(135deg, var(--verde-escuro), #0f3025);
            color: white;
            text-align: center;
            padding: 3rem 1rem;
            box-shadow: 0 4px 15px rgba(0,0,0,0.15);
        }

        header h1 {
            margin: 0;
            font-size: 2.2rem;
            letter-spacing: 0.5px;
        }

        header p {
            margin: 0.5rem 0 0 0;
            font-size: 1.2rem;
            opacity: 0.85;
        }

        .container {
            max-width: 900px;
            margin: -2rem auto 3rem auto;
            padding: 0 1.5rem;
        }

        /* --- CARDS E ENTRADAS --- */
        .painel, .resultados {
            background-color: var(--card-fundo);
            padding: 2.5rem;
            border-radius: 12px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.05);
            margin-bottom: 2rem;
        }

        h2 {
            color: var(--verde-escuro);
            margin-top: 0;
            font-size: 1.5rem;
            border-bottom: 2px solid #e8f5e9;
            padding-bottom: 0.7rem;
        }

        .input-group {
            margin: 1.5rem 0;
        }

        label {
            font-weight: 600;
            display: block;
            margin-bottom: 0.5rem;
        }

        input {
            width: 100%;
            padding: 1rem;
            font-size: 1.1rem;
            border: 2px solid #cbd5e1;
            border-radius: 6px;
            box-sizing: border-box;
            transition: all 0.3s;
        }

        input:focus {
            border-color: var(--verde-claro);
            outline: none;
            box-shadow: 0 0 0 4px rgba(39, 174, 96, 0.1);
        }

        .dica {
            font-size: 0.9rem;
            color: #64748b;
            margin-top: 0.5rem;
            font-style: italic;
        }

        button {
            background-color: var(--marrom);
            color: white;
            border: none;
            padding: 1rem 2rem;
            font-size: 1.1rem;
            font-weight: bold;
            border-radius: 6px;
            cursor: pointer;
            width: 100%;
            transition: all 0.3s;
            box-shadow: 0 4px 6px rgba(139, 90, 43, 0.2);
        }

        button:hover {
            background-color: var(--verde-claro);
            box-shadow: 0 4px 12px rgba(39,
